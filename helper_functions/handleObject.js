const groupIntoLargeObject = (object) => {
    let returnObject = {};
    let length = object.length;
    for (let i = 0; i < length; i++) {
        Object.assign(returnObject, )
    }
    return returnObject;
}

